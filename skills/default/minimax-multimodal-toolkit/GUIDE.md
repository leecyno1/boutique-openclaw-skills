# minimax-multimodal-toolkit 使用指南

## 1. 功能定位
- Use mmx to generate text, images, video, speech, and music via the MiniMax AI platform. Use when the user wants to create media content, chat with MiniMax models, perform web se...
- 默认档位: 仅全量默认包/手动同步
- 仓库目录: `skills/default/minimax-multimodal-toolkit`
- 安装后目录: `~/.openclaw/skills/minimax-multimodal-toolkit`

## 2. 使用前准备
- 无强制 API Key；按 skill 自身依赖运行。

## 3. 配置步骤
1. 通常无需额外配置。若运行时报缺依赖，再按 `SKILL.md` 补装。

## 4. 推荐提问方式
- 请使用 minimax-multimodal-toolkit 帮我处理当前任务。
- 如果 minimax-multimodal-toolkit 需要额外配置，请先告诉我缺少什么。

## 5. 手动验证
```bash
ls -la ~/.openclaw/skills/minimax-multimodal-toolkit
```

## 6. 参考资料
- 上游来源: https://github.com/MiniMax-AI/skills/tree/main/skills/minimax-multimodal-toolkit
- 本技能说明: `SKILL.md`
