# baoyu-compress-image 使用指南

## 1. 功能定位
- Compresses images to WebP (default) or PNG with automatic tool selection. Use when user asks to "compress image", "optimize image", "convert to webp", or reduce image file size.
- 默认档位: 仅全量默认包/手动同步
- 仓库目录: `skills/default/baoyu-compress-image`
- 安装后目录: `~/.openclaw/skills/baoyu-compress-image`

## 2. 使用前准备
- 无强制 API Key；按 skill 自身依赖运行。

## 3. 配置步骤
1. 通常无需额外配置。若运行时报缺依赖，再按 `SKILL.md` 补装。

## 4. 推荐提问方式
- 请使用 baoyu-compress-image 帮我处理当前任务。
- 如果 baoyu-compress-image 需要额外配置，请先告诉我缺少什么。

## 5. 手动验证
```bash
ls -la ~/.openclaw/skills/baoyu-compress-image
```

## 6. 参考资料
- 上游来源: https://github.com/JimLiu/baoyu-skills/tree/main/skills/baoyu-compress-image
- 本技能说明: `SKILL.md`
