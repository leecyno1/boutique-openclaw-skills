---
name: dasheng-caiji
description: 已并入 `dasheng-daily-intake` / `daily intake`。保留此 Skill 仅用于兼容旧叫法；凡是“大圣采集/采集环节/caiji”相关调用，一律转到 `dasheng-daily-intake`。
---

# dasheng-caiji（兼容别名）

本 Skill 已不再单独维护。

## 合并说明
- 历史上的 `dasheng-caiji` 与 `dasheng-daily-intake` 职责重复
- 现已统一并入：**`dasheng-daily-intake`（对外打包名：`daily intake`）**
- 后续凡是：
  - 大圣采集
  - caiji
  - 采集环节
  - 第一环节
  都按 **`dasheng-daily-intake` / `daily intake`** 执行

## 当前唯一入口
请改用：`dasheng-daily-intake`
